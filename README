
->gerer env -i (creer les trois variables env)
->gere ${}
->gerer $() ()
->trouver un solution pour la variable $_
->ajouter les check de parsing pour export
	(premier char is alpha ou _ et only alphanumerique ou _) Done a check

->revoir la gestion des espaces sur echo | Done (a recheck quand meme)
->ameliorer les commande not found | Done
->ameliorer les redirection (ls < exit) | Done
->ameliorer les redirection| Done
	->ameliorer "<<"
	->ameliorer >
->ameliorer export (+=, alphanum) | Done a check par reda
->gestion du absolute PATH sur cd | Done
->blinder Makefile
->reparer env
->rev $PWD
->export b=" -l" puis ls$PWD
